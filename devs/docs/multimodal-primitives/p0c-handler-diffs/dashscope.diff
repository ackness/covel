diff --color -ruN /Users/wuyong/.covel/plugins/dashscope-image-gen.bak-p0c-20260426-185751/runtimes/image-generator/handler.js /Users/wuyong/.covel/plugins/dashscope-image-gen/runtimes/image-generator/handler.js
--- /Users/wuyong/.covel/plugins/dashscope-image-gen.bak-p0c-20260426-185751/runtimes/image-generator/handler.js	2026-04-26 18:57:51
+++ /Users/wuyong/.covel/plugins/dashscope-image-gen/runtimes/image-generator/handler.js	2026-04-26 19:03:30
@@ -17,6 +17,14 @@
  * and defers the call so the HTTP response flushes immediately. Right-
  * panel gallery subscribes to `plugin-data.changed` SSE on the `images`
  * namespace and re-renders when the commit lands.
+ *
+ * P0-c (multimodal primitives): the OSS URL returned by DashScope expires
+ * after 24h, so this handler now ingests every URL into the framework's
+ * `MediaStore` via `ctx.media.ingestUrl(...)`. The `images` namespace
+ * record stores a `MediaRef` (`{ id, mime, size }`) instead of the raw
+ * URL / base64 / mimeType triplet, and the handler additionally emits an
+ * `asset.generate` proposal so the kernel can fan the asset out to trace
+ * + SSE + render pipelines (see SPEC §5.7).
  */

 const PROMPT_RUNTIME_ID = 'dashscope-image-gen/prompt-generator';
@@ -28,14 +36,6 @@
 const POLL_INTERVAL_MS = 2000;
 const POLL_MAX_ATTEMPTS = 30; // ~60s ceiling

-/**
- * DashScope async result URLs expire 24 h after generation per the
- * provider's text-to-image documentation. Stamp `expiresAt` so the
- * gallery UI / future asset-cache can surface a "URL expired,
- * regenerate" state instead of a broken image once the OSS link 404s.
- */
-const DASHSCOPE_URL_TTL_MS = 24 * 60 * 60 * 1000;
-
 /** wan2.7-image series rejects negative_prompt — strip silently. */
 function modelRejectsNegativePrompt(model) {
   return typeof model === 'string' && model.startsWith('wan2.7-image');
@@ -215,7 +215,7 @@

 /** @type {import('@covel/plugin-loader').FunctionHandler} */
 export default async function imageGeneratorHandler(ctx) {
-  const { turnId, gateway, utils, userSettings, pluginData, logger } = ctx;
+  const { turnId, gateway, utils, userSettings, pluginData, logger, media } = ctx;

   if (!gateway || typeof gateway.resolveSlot !== 'function') {
     return {
@@ -226,6 +226,15 @@
     };
   }

+  if (!media || typeof media.ingestUrl !== 'function' || typeof media.put !== 'function') {
+    return {
+      status: 'failed',
+      error:
+        'ctx.media.{ingestUrl,put} is unavailable. This plugin requires Covel ' +
+        'framework with the MediaStore primitive (P0-a). Upgrade @covel/runtime / @covel/store.',
+    };
+  }
+
   const extracted = extractPrompt(ctx);
   if (!extracted) {
     await logger?.warn?.('no-prompt-found', {
@@ -349,30 +358,74 @@
         completedAt,
       );
     }
-    const expiresAt = new Date(Date.parse(completedAt) + DASHSCOPE_URL_TTL_MS).toISOString();
+
+    // P0-c: ingest the OSS URL (24h expiry) into MediaStore so the bytes
+    // become content-addressable + permanent. `record.ref` replaces the
+    // legacy `url` / `base64` / `mimeType` / `expiresAt` fields.
+    let primaryRef;
+    const refs = [];
+    try {
+      for (const url of urls) {
+        const ref = await media.ingestUrl(url, {
+          allowedMimes: ['image/png', 'image/jpeg', 'image/webp'],
+          meta: {
+            prompt,
+            model: resolvedModel,
+            provider: slot.provider,
+            imageId,
+          },
+        });
+        refs.push(ref);
+      }
+      primaryRef = refs[0];
+    } catch (err) {
+      return failureRecord(
+        pluginData,
+        logger,
+        baseRecord,
+        `Failed to ingest DashScope OSS URL into MediaStore: ${
+          err instanceof Error ? err.message : String(err)
+        }`,
+        completedAt,
+      );
+    }
+
     const record = {
       ...baseRecord,
       status: 'done',
-      url: firstUrl,
-      base64: null,
-      mimeType: 'image/png',
-      all: urls.map((url) => ({ url, mimeType: 'image/png' })),
+      ref: primaryRef,
+      ...(refs.length > 1 ? { refs } : {}),
       completedAt,
       durationMs: Date.parse(completedAt) - Date.parse(startedAt),
-      expiresAt,
     };
     await logger?.info?.('image.generate.completed', {
       imageId,
       durationMs: record.durationMs,
       imageCount: urls.length,
+      refId: primaryRef?.id,
     });
     return {
       imageId,
       status: 'done',
-      url: firstUrl,
+      ref: primaryRef,
       prompt,
       promptMode,
       pluginData: [{ namespace: IMAGES_NAMESPACE, key: imageId, value: record }],
+      // The kernel reads `output.assets[]` (and `output.assetGenerations[]`)
+      // and turns each entry into a Proposal { type: 'asset.generate' }.
+      // Returning a top-level `proposals[]` would be silently dropped — see
+      // packages/runtime/src/session-kernel.ts collectAssetGenerations.
+      assets: refs.map((ref, idx) => ({
+        ref,
+        modality: 'image',
+        meta: {
+          prompt,
+          model: resolvedModel,
+          provider: slot.provider,
+          imageId,
+          ...(refs.length > 1 ? { imageIndex: idx } : {}),
+        },
+      })),
     };
   } catch (err) {
     return failureRecord(
diff --color -ruN /Users/wuyong/.covel/plugins/dashscope-image-gen.bak-p0c-20260426-185751/runtimes/image-generator/ui/gallery.json /Users/wuyong/.covel/plugins/dashscope-image-gen/runtimes/image-generator/ui/gallery.json
--- /Users/wuyong/.covel/plugins/dashscope-image-gen.bak-p0c-20260426-185751/runtimes/image-generator/ui/gallery.json	2026-04-26 18:57:51
+++ /Users/wuyong/.covel/plugins/dashscope-image-gen/runtimes/image-generator/ui/gallery.json	2026-04-26 18:59:05
@@ -56,14 +56,13 @@
                 ]
               },
               {
-                "component": "Image",
+                "component": "Media",
                 "props": {
-                  "src": { "$item": "value/url" },
-                  "base64": { "$item": "value/base64" },
-                  "mimeType": { "$item": "value/mimeType" },
+                  "src": { "$item": "value/ref" },
                   "alt": { "$item": "value/prompt" },
                   "aspectRatio": "1/1",
-                  "rounded": "md"
+                  "rounded": "md",
+                  "as": "image"
                 }
               },
               {
