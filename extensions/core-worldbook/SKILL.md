# core-worldbook

Injects curated worldbook facts into the session context when the active world or scene requires them.

It also stages world seed content that can be inspected and later imported into
`World + Artifact + MemoryDocument` through package-owned tooling.
