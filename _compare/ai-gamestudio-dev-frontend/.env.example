# Default API base (same origin)
VITE_API_BASE_URL=/api

# Local dev default: websocket
VITE_CHAT_TRANSPORT=websocket

# Optional: override websocket endpoint prefix
# VITE_WS_BASE_URL=ws://localhost:8000/ws

