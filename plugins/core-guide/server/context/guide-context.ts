import type { ContextProviderInput } from "@covel/plugin-runtime";

/**
 * Placeholder context provider for the guide plugin.
 */
export async function guideContextProvider(_input: ContextProviderInput) {
  return null;
}
