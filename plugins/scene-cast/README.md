# scene-cast

记录当前场景里谁在场，以及谁可能正在说话。

## 运行时结构

- `PLUGIN.md`：定时函数 runtime 声明。
- `handler.js`：根据会话消息和角色记录计算当前演员上下文。
- `ui/scene-cast-panel.json`：右侧场景演员面板。

## 数据与行为

- 为 `chat-mode-narrator` 输出压缩的当前演员上下文。
- 执行确定性分析，不调用模型。
- 用于对话模式插件组合。

## 开发

修改演员选择逻辑或 UI 数据路径后，运行本插件测试。
