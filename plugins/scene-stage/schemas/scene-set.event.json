{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["location", "timeOfDay"],
  "properties": {
    "location": {
      "type": "string",
      "minLength": 1,
      "description": "Scene/location name as it appears in the narrative."
    },
    "timeOfDay": { "enum": ["day", "night"] },
    "visualHint": {
      "type": "string",
      "description": "One English sentence describing the visuals — REQUIRED when the location is new to this world."
    }
  }
}
